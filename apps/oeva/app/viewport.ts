import type {Viewport} from "next";

const viewport: Viewport = {
    width: "device-width",
    userScalable: false,
    initialScale: 1,
    minimumScale: 1,
    maximumScale: 1,
    viewportFit: "cover",
    themeColor: '#000000',
}

export default viewport